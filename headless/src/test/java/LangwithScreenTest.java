import io.github.team9.escapefromuni.LangwithScreen;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class LangwithScreenTest extends BaseTest{

    /**
     * Test that LangwithScreen can be instantiated with mocked dependencies.
     */
    @Test
    public void testclassexists() {
        // This test simply verifies the class exists and can be referenced
        assertNotNull(LangwithScreen.class);
    }

}
